{
  "name": "backend",
  "devDependencies": {
    "serverless-offline": "^12.0.4",
    "serverless-python-requirements": "^6.0.0",
    "serverless-dynamodb-local": "^0.2.40"
  },
  "scripts": {
    "start": "sls offline",
    "deploy": "sls deploy"
  }
}
